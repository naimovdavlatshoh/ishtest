import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors
  // Main brand blue
  static const Color primary = Color(0xFF2563EB);
  static const Color primaryDark = Color(0xFF1D4ED8);
  static const Color primaryLight = Color(0xFF60A5FA);

  // Background Colors
  // Very light bluish background behind cards/screens
  static const Color background = Color(0xFFF1F5FF);
  static const Color surface = Colors.white;
  static const Color surfaceVariant = Color(0xFFEFF3FF);

  // Text Colors
  static const Color textPrimary = Color(0xFF000000);
  static const Color textSecondary = Color(0xFF666666);
  static const Color textTertiary = Color(0xFF999999);

  // Accent Colors
  static const Color success = Color(0xFF057642);
  static const Color error = Color(0xFFCC1016);
  static const Color warning = Color(0xFFF5C26B);
  static const Color info = Color(0xFF0073B1);

  // Divider & Border
  static const Color divider = Color(0xFFE0E0E0);
  static const Color border = Color(0xFFCCCCCC);

  // Icon Colors
  static const Color iconPrimary = Color(0xFF666666);
  static const Color iconSecondary = Color(0xFF999999);

  // Special Colors
  static const Color online = Color(0xFF30B566);
  static const Color offline = Color(0xFF999999);

  // Reaction Colors
  static const Color like = Color(0xFF0073B1);
  static const Color love = Color(0xFFDF704D);
  static const Color celebrate = Color(0xFF6DAE4F);
  static const Color support = Color(0xFF8A4FE4);
}
